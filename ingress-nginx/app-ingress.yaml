apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: testapi
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: testapi
              servicePort: 8080
